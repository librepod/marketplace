# User Apps Source — GitRepository pointing to in-cluster Gogs.
# Depends on system-configs (which depend on system-apps).
---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: user-apps-source
  namespace: flux-system
spec:
  dependsOn:
    - name: system-configs
    - name: gogs
  interval: 1h
  retryInterval: 2m
  timeout: 5m
  sourceRef:
    kind: OCIRepository
    name: librepod-bootstrap
  path: ./infrastructure/user-apps-source
  prune: true
  patches: []
  postBuild:
    substitute:
      BASE_DOMAIN: "librepod.local"

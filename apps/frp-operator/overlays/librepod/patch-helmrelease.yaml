apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: frp-operator
spec:
  chart:
    spec:
      chart: frp-operator
      version: "~1.5.0"
      sourceRef:
        kind: HelmRepository
        name: frp-operator-charts
      interval: 12h
  # Fork image with fix for https://github.com/zufardhiyaulhaq/frp-operator/pull/17
  # TODO: Revert to upstream once the PR is merged.
  values:
    operator:
      image: ghcr.io/librepod/frp-operator
      tag: v0.7.0-librepod1

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: frp-operator

resources:
- namespace.yaml
- helmrepository.yaml
# helmrelease.yaml is intentionally excluded here because the librepod overlay
# provides its own full HelmRelease using a fork OCI chart (see overlays/librepod/
# kustomization.yaml for details). Once upstream PR #17 is merged, uncomment this
# and the overlay will switch to a patch instead of a full override.
# - helmrelease.yaml

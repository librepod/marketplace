# Non-secret Xray Checker configuration. Everything not set here uses
# upstream defaults. SUBSCRIPTION_URL lives in the secret, not here.
# Safe to show server details: dashboard sits behind oauth2-proxy SSO.
WEB_SHOW_DETAILS=true

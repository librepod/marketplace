apiVersion: v1
kind: Secret
metadata:
  name: xray-checker-secret
type: Opaque
stringData:
  # Subscription URL embeds credentials — user-supplied at install time,
  # injected by Flux postBuild.substituteFrom (see metadata.yaml).
  SUBSCRIPTION_URL: "${SUBSCRIPTION_URL}"

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: headscale-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # storageClassName set in overlay via patch-storage-class.yaml

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: headplane-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # No storageClassName here — patched in by overlay

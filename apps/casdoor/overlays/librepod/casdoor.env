# Casdoor app.conf overrides, loaded as env vars via the chart's envFrom.
# (Casdoor overrides any app.conf key with an env var of the same name.)
dataSourceName=file:/data/casdoor.db?cache=shared
runmode=prod

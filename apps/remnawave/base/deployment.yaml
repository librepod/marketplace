apiVersion: apps/v1
kind: Deployment
metadata:
  name: remnawave
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: remnawave
  template:
    metadata:
      labels:
        app.kubernetes.io/name: remnawave
    spec:
      containers:
        - name: remnawave
          image: remnawave/backend
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: remnawave-env
          ports:
            - name: http
              containerPort: 3000
            - name: metrics
              containerPort: 3001
          # /health on the metrics port pings Postgres (terminus PrismaHealthIndicator),
          # matching the upstream docker-compose healthcheck.
          startupProbe:
            httpGet:
              path: /health
              port: metrics
            periodSeconds: 5
            failureThreshold: 30
          readinessProbe:
            httpGet:
              path: /health
              port: metrics
            periodSeconds: 10
            failureThreshold: 3
          volumeMounts:
            - name: valkey-socket
              mountPath: /var/run/valkey
        # Valkey over a unix socket — mirrors the upstream compose "remnawave-redis"
        # service (no TCP port, no persistence; cache only).
        - name: valkey
          image: valkey/valkey
          imagePullPolicy: IfNotPresent
          command:
            - valkey-server
            - --save
            - ""
            - --appendonly
            - "no"
            - --maxmemory-policy
            - noeviction
            - --loglevel
            - warning
            - --unixsocket
            - /var/run/valkey/valkey.sock
            - --unixsocketperm
            - "777"
            - --port
            - "0"
          volumeMounts:
            - name: valkey-socket
              mountPath: /var/run/valkey
      volumes:
        - name: valkey-socket
          emptyDir: {}

POSTGRES_DB=remnawave
POSTGRES_USER=postgres
# Substituted by Flux postBuild from Secret/remnawave-config — must stay in
# sync with DATABASE_URL in base/remnawave.env (same ${DB_PASSWORD}).
POSTGRES_PASSWORD=${DB_PASSWORD}

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: remnawave-tailscale-state
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  # storageClassName patched in by the enabling overlay (nfs-client)

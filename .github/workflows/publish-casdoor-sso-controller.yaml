name: Publish casdoor-sso-controller image

on:
  push:
    branches: [master]
    paths:
      - 'casdoor-sso-controller/**'
      - '.github/workflows/publish-casdoor-sso-controller.yaml'
  pull_request:
    paths:
      - 'casdoor-sso-controller/**'
      - '.github/workflows/publish-casdoor-sso-controller.yaml'
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-go@v5
        with: { go-version: '1.25' }
      - name: Run tests (envtest via make)
        run: cd casdoor-sso-controller && make test

  build-and-sign:
    needs: test
    if: github.event_name == 'push' && github.ref == 'refs/heads/master'
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v6
      - uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: sigstore/cosign-installer@v4.1.1
        with: { cosign-release: 'v3.0.6' }
      - name: Build & push
        id: img
        run: |
          TAG="0.2.2"
          docker buildx build --push \
            --metadata-file metadata.json \
            -t ghcr.io/librepod/casdoor-sso-controller:${TAG} \
            -t ghcr.io/librepod/casdoor-sso-controller:latest \
            casdoor-sso-controller
          # Read the pushed manifest digest from buildx's metadata file. This is
          # version-stable; the previous `imagetools inspect --format
          # '{{.manifest.digest}}'` broke when buildx changed the template context
          # (the `manifest` field is gone from imagetools.tplInput), leaving the
          # image pushed-but-unsigned. cosign signs <image>@<this digest>.
          DIGEST=$(jq -r '."containerimage.digest"' metadata.json)
          test -n "$DIGEST"
          echo "digest=$DIGEST" >> "$GITHUB_OUTPUT"
      - name: Sign (key-based, matches cluster cosign-pub)
        env:
          COSIGN_PRIVATE_KEY: ${{ secrets.COSIGN_PRIVATE_KEY }}
          COSIGN_PASSWORD: ${{ secrets.COSIGN_PASSWORD }}
        run: |
          cosign sign --key env://COSIGN_PRIVATE_KEY --yes \
            ghcr.io/librepod/casdoor-sso-controller@${{ steps.img.outputs.digest }}

  # Publish a PR-tagged image for branch/PR testing. Unsigned by design: the dev
  # cluster runs no image-signature admission controller, so signing is unenforced
  # here and would only slow the PR run. Released tags (0.2.2/latest) are still
  # produced + key-signed by build-and-sign on master.
  build-pr:
    needs: test
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    permissions:
      packages: write
      contents: read
    steps:
      - uses: actions/checkout@v6
      - uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build & push PR-tagged image
        run: |
          TAG="pr-${{ github.event.pull_request.number }}"
          docker buildx build --push \
            -t ghcr.io/librepod/casdoor-sso-controller:${TAG} \
            casdoor-sso-controller
          echo "Published ghcr.io/librepod/casdoor-sso-controller:${TAG}"

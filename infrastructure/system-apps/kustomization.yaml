apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - casdoor.yaml
  - cert-manager.yaml
  # - flux-operator-mcp.yaml
  # - frpc.yaml  # Removed: requires FRP auth token, will be a user app once LibrePort integration is ready
  - frp-operator.yaml
  - gogs.yaml
  - storage.yaml
  - oauth2-proxy.yaml
  - reflector.yaml
  - step-certificates.yaml
  - step-issuer.yaml
  - traefik.yaml

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: gogs

resources:
  - serviceaccount.yaml
  - role.yaml
  - rolebinding.yaml
  # job.yaml is added in Task 4.

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator: []  # the script ConfigMap is added in Task 3.

---
phase: 02-catalog-ui
plan: 06
type: execute
wave: 4
depends_on:
  - 02-02
  - 02-04
  - 02-05
files_modified:
  - ui/packages/client/src/components/AppCard.test.tsx
  - ui/packages/client/src/components/AppCardSkeleton.test.tsx
  - ui/packages/client/src/components/ErrorBlock.test.tsx
  - ui/packages/client/src/pages/CatalogPage.test.tsx
  - ui/packages/client/src/pages/AppDetailPage.test.tsx
  - ui/package.json
autonomous: true
requirements:
  - CAT-01
  - CAT-02
  - CAT-03
  - STAT-02

must_haves:
  truths:
    - "All five test files pass with zero failures"
    - "npm run test --workspace=packages/client exits 0"
    - "Test coverage includes: card renders name/desc/badge (CAT-01, CAT-03), 12 skeletons while loading (STAT-02), error block retry (STAT-02), detail page fields (CAT-02), 404 not-found (CAT-02)"
    - "Workspace root scripts include client test command"
  artifacts:
    - path: "ui/packages/client/src/components/AppCard.test.tsx"
      provides: "All AppCard tests passing"
      contains: "renders app display name"
    - path: "ui/packages/client/src/pages/CatalogPage.test.tsx"
      provides: "All CatalogPage tests passing"
      contains: "toHaveLength(12)"
    - path: "ui/packages/client/src/pages/AppDetailPage.test.tsx"
      provides: "All AppDetailPage tests passing"
      contains: "← Back to catalog"
  key_links:
    - from: "ui/package.json"
      to: "ui/packages/client"
      via: "npm run test:client workspace script"
      pattern: "test:client"
---

<objective>
Run the full test suite and fix any failures. Test files were written in Plan 02 with exact behavioral expectations; components were implemented in Plans 04 and 05. This plan verifies all tests pass and wires the client test command into the workspace root scripts.

Purpose: Phase gate — tests must be green before the phase is considered complete. Any gap between test expectations and implementations is discovered and fixed here.
Output: All five test files passing; workspace root has a test:client convenience script.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-catalog-ui/02-CONTEXT.md
@.planning/phases/02-catalog-ui/02-RESEARCH.md

<interfaces>
<!-- Test runner commands -->
Run from ui/ workspace root:

# Run client tests only
npm run test --workspace=packages/client -- --run

# Run with verbose output
npm run test --workspace=packages/client -- --run --reporter=verbose

# Run with coverage
npm run test --workspace=packages/client -- --run --coverage

# Run a single test file
npm run test --workspace=packages/client -- --run src/components/AppCard.test.tsx
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and fix all failures</name>
  <files>
    ui/packages/client/src/components/AppCard.test.tsx
    ui/packages/client/src/components/AppCardSkeleton.test.tsx
    ui/packages/client/src/components/ErrorBlock.test.tsx
    ui/packages/client/src/pages/CatalogPage.test.tsx
    ui/packages/client/src/pages/AppDetailPage.test.tsx
  </files>
  <read_first>
    - ui/packages/client/src/components/AppCard.test.tsx (read actual test content before running)
    - ui/packages/client/src/pages/CatalogPage.test.tsx (read actual test content)
    - ui/packages/client/src/pages/AppDetailPage.test.tsx (read actual test content)
    - ui/packages/client/src/components/AppCard.tsx (read actual implementation)
    - ui/packages/client/src/pages/CatalogPage.tsx (read actual implementation)
    - ui/packages/client/src/pages/AppDetailPage.tsx (read actual implementation)
    - .planning/phases/02-catalog-ui/02-UI-SPEC.md (Copywriting Contract — exact strings that tests assert against)
  </read_first>
  <action>
Run the full test suite, then fix any failures.

Step 1 — Run all client tests:
```bash
cd /home/alex/code/librepod/marketplace/ui
npm run test --workspace=packages/client -- --run --reporter=verbose
```

Step 2 — For each failing test, diagnose the root cause:

**Common failure patterns and fixes:**

a) **Import fails — component file doesn't exist:** Indicates Plan 04 or 05 was not executed. Check that all component files are present. If a component is missing, create it following the exact patterns from the plan that should have created it.

b) **"Cannot find module @librepod/shared":** vitest.config.ts alias for @librepod/shared is missing or incorrect. Verify `resolve.alias["@librepod/shared"]` points to `../shared/src/types.ts`.

c) **"toBeInTheDocument is not a function":** src/test/setup.ts is not being loaded. Verify `setupFiles: ["./src/test/setup.ts"]` in vitest.config.ts and that setup.ts contains `import "@testing-library/jest-dom"`.

d) **Skeleton count test fails (not 12):** Either `data-testid="app-card-skeleton"` is missing from AppCardSkeleton.tsx, or the test selector doesn't match. Fix the selector or add the data-testid.

e) **"← Back to catalog" not found:** AppDetailPage.tsx uses different copy. Fix to match exact string from UI-SPEC Copywriting Contract.

f) **Install button not disabled:** AppDetailPage.tsx button missing `disabled` attribute. Add `disabled` prop to the Button component call.

g) **"NOT_FOUND" error not detected:** The error message comparison `error?.message === "NOT_FOUND"` doesn't match what was thrown. Ensure the queryFn throws `new Error("NOT_FOUND")` for 404 responses, and the component checks this exact message.

h) **TanStack Query test wrapper issues:** If tests fail with QueryClient errors, verify the `createWrapper()` helper in test files creates a fresh `QueryClient` per test with `defaultOptions: { queries: { retry: 0 } }`.

Step 3 — After fixing, re-run the full suite:
```bash
cd /home/alex/code/librepod/marketplace/ui
npm run test --workspace=packages/client -- --run --reporter=verbose
```

Step 4 — All tests must pass before proceeding. Do not proceed to Task 2 if any test fails.
  </action>
  <verify>
    <automated>cd /home/alex/code/librepod/marketplace/ui && npm run test --workspace=packages/client -- --run 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test --workspace=packages/client -- --run` exits with code 0
    - Output contains `Tests X passed` with no failed tests
    - AppCard.test.tsx: all 5 tests pass (name, description, category badge, icon img, no-version)
    - AppCardSkeleton.test.tsx: both tests pass (renders, 200px height)
    - ErrorBlock.test.tsx: all 4 tests pass (heading, body, button, onRetry called)
    - CatalogPage.test.tsx: all 5 tests pass (12 skeletons, renders cards, error block, empty state, page title)
    - AppDetailPage.test.tsx: all 5 tests pass (detail fields, back link, disabled install, view source, not-found)
    - Zero test suites failing
  </acceptance_criteria>
  <done>All 21 tests across 5 files pass with zero failures.</done>
</task>

<task type="auto">
  <name>Task 2: Wire client test command into workspace root and verify dev build</name>
  <files>
    ui/package.json
  </files>
  <read_first>
    - ui/package.json (current workspace root scripts — must read before modifying)
    - .planning/phases/02-catalog-ui/02-PATTERNS.md (npm scripts pattern section)
  </read_first>
  <action>
Update ui/package.json to add client-specific test and build scripts, then verify the Vite dev server starts.

Step 1 — Read the current ui/package.json scripts section. The existing scripts are:
```json
"scripts": {
  "dev": "npm run dev --workspace=packages/server",
  "build": "npm run build --workspace=packages/server",
  "test": "npm run test --workspace=packages/server",
  "test:e2e": "npm run test:e2e --workspace=packages/server"
}
```

Add client-targeted scripts alongside the existing server scripts:
```json
"scripts": {
  "dev": "npm run dev --workspace=packages/server",
  "dev:client": "npm run dev --workspace=packages/client",
  "build": "npm run build --workspace=packages/server",
  "build:client": "npm run build --workspace=packages/client",
  "test": "npm run test --workspace=packages/server",
  "test:client": "npm run test --workspace=packages/client -- --run",
  "test:client:watch": "npm run test --workspace=packages/client",
  "test:e2e": "npm run test:e2e --workspace=packages/server"
}
```

Step 2 — Verify the Vite dev server can start (TypeScript compilation check):
```bash
cd /home/alex/code/librepod/marketplace/ui/packages/client
npx tsc --noEmit -p tsconfig.app.json 2>&1 | head -20
```

If TypeScript reports errors, fix them. Common issues:
- Missing imports in page/component files
- Type errors from test files (test files should use tsconfig.app.json `include` which covers src/**/* — tests in src/ are included)
- Path alias not resolving: verify both tsconfig.json and tsconfig.app.json have `@/*` and `@librepod/shared` paths

Step 3 — Do NOT start a long-running dev server process (would block). Only run the tsc --noEmit check.
  </action>
  <verify>
    <automated>cd /home/alex/code/librepod/marketplace/ui && npm run test:client 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ui/package.json contains `"test:client": "npm run test --workspace=packages/client -- --run"` script
    - ui/package.json contains `"dev:client": "npm run dev --workspace=packages/client"` script
    - `npm run test:client` from ui/ exits 0 (all client tests pass)
    - `npx tsc --noEmit -p tsconfig.app.json` in ui/packages/client exits 0 (no TypeScript errors)
    - ui/package.json still contains all original server scripts (dev, build, test, test:e2e) unchanged
  </acceptance_criteria>
  <done>Workspace root has test:client and dev:client convenience scripts; tsc --noEmit exits clean; all tests confirmed passing via npm run test:client.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process → vi.spyOn(global.fetch) | Test-only interception; no real network requests in tests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-T07 | Tampering | vi.spyOn mocking in tests | accept | Test-only pattern; production code uses real fetch; mocking scope is per-test with beforeEach reset |
</threat_model>

<verification>
Phase 2 gate checks — run these after Plan 06 completes:

1. `npm run test:client` from ui/ exits 0 — full client test suite passes
2. `npx tsc --noEmit -p tsconfig.app.json` from ui/packages/client exits 0
3. `ls ui/packages/client/src/components/ui/` shows all 5 shadcn components
4. `grep "classList.add" ui/packages/client/src/main.tsx` confirms dark mode init
5. `grep "/apps/:name" ui/packages/client/src/router.tsx` confirms detail page route
6. `grep "← Back to catalog" ui/packages/client/src/pages/AppDetailPage.tsx` confirms UI-SPEC copy

Phase success criteria summary:
- CAT-01: AppCard renders icon, name, description, category label; CatalogPage renders grid
- CAT-02: AppDetailPage renders full app info at /apps/:name; back button works via React Router Link
- CAT-03: AppCard uses Badge variant="secondary" for category; confirmed in tests
- STAT-02: 12 skeleton cards during isPending; ErrorBlock with retry on isError
</verification>

<success_criteria>
- All 21 tests across 5 test files pass: 0 failures, 0 errors
- TypeScript compiles clean: tsc --noEmit exits 0
- npm run test:client convenience script works from workspace root
- Phase 2 is feature-complete: catalog grid, detail pages, loading states, browser navigation all implemented and tested
</success_criteria>

<output>
After completion, create `.planning/phases/02-catalog-ui/02-06-SUMMARY.md`
</output>

---
phase: 02-catalog-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ui/packages/client/package.json
  - ui/packages/client/tsconfig.json
  - ui/packages/client/tsconfig.app.json
  - ui/packages/client/tsconfig.node.json
  - ui/packages/client/vite.config.ts
  - ui/packages/client/vitest.config.ts
  - ui/packages/client/index.html
  - ui/packages/client/components.json
  - ui/packages/client/src/index.css
  - ui/packages/client/src/lib/utils.ts
  - ui/packages/client/src/components/ui/badge.tsx
  - ui/packages/client/src/components/ui/button.tsx
  - ui/packages/client/src/components/ui/card.tsx
  - ui/packages/client/src/components/ui/skeleton.tsx
  - ui/packages/client/src/components/ui/separator.tsx
autonomous: true
requirements:
  - CAT-01
  - CAT-02
  - CAT-03
  - STAT-02

must_haves:
  truths:
    - "vite dev server starts without errors from ui/packages/client/"
    - "TypeScript compiles: tsc --noEmit resolves @/* and @librepod/shared paths without errors"
    - "Vitest test runner can be invoked and finds test files matching src/**/*.test.tsx"
    - "shadcn Badge, Button, Card, Skeleton, Separator components exist in src/components/ui/ with CSS variable tokens"
    - "Tailwind v4 CSS variables are set in src/index.css (dark mode tokens present)"
  artifacts:
    - path: "ui/packages/client/package.json"
      provides: "All runtime + dev dependencies declared"
      contains: "react-router-dom"
    - path: "ui/packages/client/vite.config.ts"
      provides: "Vite build + dev server config with Tailwind plugin + /api proxy"
      contains: "@tailwindcss/vite"
    - path: "ui/packages/client/vitest.config.ts"
      provides: "Vitest config with jsdom environment"
      contains: "jsdom"
    - path: "ui/packages/client/tsconfig.app.json"
      provides: "TypeScript config for source files with path aliases"
      contains: "@librepod/shared"
    - path: "ui/packages/client/src/index.css"
      provides: "Tailwind v4 CSS variable token system (dark + light)"
      contains: "@import \"tailwindcss\""
    - path: "ui/packages/client/src/lib/utils.ts"
      provides: "cn() class merging helper"
      contains: "clsx"
  key_links:
    - from: "ui/packages/client/vite.config.ts"
      to: "http://localhost:3000"
      via: "server.proxy /api"
      pattern: "proxy.*localhost:3000"
    - from: "ui/packages/client/tsconfig.app.json"
      to: "ui/packages/shared/src/types.ts"
      via: "@librepod/shared path alias"
      pattern: "@librepod/shared.*shared/src/types.ts"
---

<objective>
Scaffold the Vite React SPA for the catalog UI. This plan turns the stub `ui/packages/client/` into a buildable, testable project with all dependencies installed, TypeScript configured, shadcn/ui initialized with Tailwind v4, and all five required shadcn components copied in.

Purpose: Every subsequent plan in this phase depends on the scaffold. Nothing can be built until dependencies are installed and the build toolchain is wired up.
Output: Working Vite project that compiles TypeScript, resolves workspace imports, and serves a dev page. shadcn components available at src/components/ui/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-catalog-ui/02-CONTEXT.md
@.planning/phases/02-catalog-ui/02-RESEARCH.md
@.planning/phases/02-catalog-ui/02-PATTERNS.md

<interfaces>
<!-- Types the executor needs. Extracted from codebase. -->

From ui/packages/shared/src/types.ts:
```typescript
export interface CatalogApp {
  name: string;
  version: string;
  displayName: string;
  description: string;
  category: string;
  icon: string;
  sourceType: string;
  sourceUrl: string;
}
export interface CatalogFile {
  apiVersion: string;
  kind: string;
  metadata: { generatedAt: string };
  apps: CatalogApp[];
}
```

From ui/packages/server/tsconfig.json (path alias pattern to replicate):
```json
"paths": {
  "@librepod/shared": ["../shared/src/types.ts"]
}
```

From ui/tsconfig.base.json (base config the client extends):
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "commonjs",
    "lib": ["ES2022"],
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write package.json and TypeScript configs</name>
  <files>
    ui/packages/client/package.json
    ui/packages/client/tsconfig.json
    ui/packages/client/tsconfig.app.json
    ui/packages/client/tsconfig.node.json
  </files>
  <read_first>
    - ui/packages/client/package.json (current stub — must read before overwriting)
    - ui/packages/server/package.json (scripts naming pattern + @librepod/shared dependency pattern)
    - ui/packages/server/tsconfig.json (path alias pattern for @librepod/shared)
    - ui/tsconfig.base.json (base config that both configs extend)
    - .planning/phases/02-catalog-ui/02-RESEARCH.md (Standard Stack table for exact versions, Pitfall 2 for two-tsconfig rule)
    - .planning/phases/02-catalog-ui/02-PATTERNS.md (Pattern Assignments for all four config files)
  </read_first>
  <action>
Write all four files. Exact content:

**ui/packages/client/package.json** — replace the stub entirely:
```json
{
  "name": "@librepod/client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev": "vite",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:cov": "vitest run --coverage"
  },
  "dependencies": {
    "@fontsource/inter": "^5.2.8",
    "@librepod/shared": "*",
    "@tanstack/react-query": "^5.99.2",
    "lucide-react": "^1.8.0",
    "react": "^19.2.5",
    "react-dom": "^19.2.5",
    "react-router-dom": "^6.30.3"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.2.2",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@testing-library/user-event": "^14.6.1",
    "@types/node": "^22.0.0",
    "@types/react": "^19.2.5",
    "@types/react-dom": "^19.2.5",
    "@vitejs/plugin-react": "^6.0.1",
    "@vitest/coverage-v8": "^3.1.0",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "jsdom": "^29.0.2",
    "tailwind-merge": "^3.5.0",
    "tailwindcss": "^4.2.2",
    "typescript": "^6.0.3",
    "vite": "^8.0.9",
    "vitest": "^4.1.4"
  }
}
```

**ui/packages/client/tsconfig.json** — root config with project references:
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"],
      "@librepod/shared": ["../shared/src/types.ts"]
    }
  },
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.node.json" }
  ],
  "files": []
}
```

**ui/packages/client/tsconfig.app.json** — source files config (Vite bundler mode):
```json
{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx",
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"],
      "@librepod/shared": ["../shared/src/types.ts"]
    },
    "noEmit": true,
    "allowImportingTsExtensions": true,
    "strict": true
  },
  "include": ["src/**/*"]
}
```
Note: override "module" and "moduleResolution" from base (base sets commonjs/node for server; client needs ESNext/bundler).

**ui/packages/client/tsconfig.node.json** — Vite config file only:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "allowSyntheticDefaultImports": true
  },
  "include": ["vite.config.ts", "vitest.config.ts"]
}
```

After writing all four files, run `npm install` from `ui/` (workspace root) to install dependencies and create the `@librepod/shared` workspace symlink:
```bash
cd /home/alex/code/librepod/marketplace/ui && npm install
```
  </action>
  <verify>
    <automated>cd /home/alex/code/librepod/marketplace/ui && npm install 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - ui/packages/client/package.json contains `"react-router-dom": "^6.30.3"` (pinned to v6)
    - ui/packages/client/package.json contains `"@tanstack/react-query": "^5.99.2"`
    - ui/packages/client/package.json contains `"@librepod/shared": "*"`
    - ui/packages/client/package.json contains `"@tailwindcss/vite"` in devDependencies
    - ui/packages/client/tsconfig.json contains `"@librepod/shared": ["../shared/src/types.ts"]`
    - ui/packages/client/tsconfig.json contains `"@/*": ["./src/*"]`
    - ui/packages/client/tsconfig.json contains `"references"` array with tsconfig.app.json and tsconfig.node.json
    - ui/packages/client/tsconfig.app.json contains `"moduleResolution": "bundler"`
    - ui/packages/client/tsconfig.app.json contains `"@librepod/shared"` path alias (BOTH tsconfig files must have it — per RESEARCH.md Pitfall 2)
    - ui/packages/client/tsconfig.app.json contains `"jsx": "react-jsx"`
    - npm install exits 0 (no dependency resolution errors)
  </acceptance_criteria>
  <done>All four config files written; npm install from ui/ workspace root completes with no errors; node_modules/@librepod/client symlink exists.</done>
</task>

<task type="auto">
  <name>Task 2: Write vite.config.ts, vitest.config.ts, and index.html</name>
  <files>
    ui/packages/client/vite.config.ts
    ui/packages/client/vitest.config.ts
    ui/packages/client/index.html
  </files>
  <read_first>
    - .planning/phases/02-catalog-ui/02-RESEARCH.md (Pattern 1 for vite.config.ts, Pattern 6 for vitest.config.ts, Pitfall 2 for alias duplication rule)
    - .planning/phases/02-catalog-ui/02-PATTERNS.md (vite.config.ts and vitest.config.ts Pattern Assignments sections)
    - ui/packages/server/vitest.config.ts (structural analog — defineConfig + globals: true)
  </read_first>
  <action>
Write three files.

**ui/packages/client/vite.config.ts** (exact content from RESEARCH.md Pattern 1):
```typescript
import path from "path"
import tailwindcss from "@tailwindcss/vite"
import react from "@vitejs/plugin-react"
import { defineConfig } from "vite"

export default defineConfig({
  plugins: [react(), tailwindcss()],
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "./src"),
    },
  },
  server: {
    proxy: {
      "/api": "http://localhost:3000",
    },
  },
})
```
Note: Do NOT add @librepod/shared alias here — it is resolved via npm workspace symlink at dev/build time. Only tsconfig and vitest.config need the explicit path alias.

**ui/packages/client/vitest.config.ts** (from RESEARCH.md Pattern 6):
```typescript
import { defineConfig } from "vitest/config"
import react from "@vitejs/plugin-react"
import path from "path"

export default defineConfig({
  plugins: [react()],
  test: {
    globals: true,
    environment: "jsdom",
    setupFiles: ["./src/test/setup.ts"],
    include: ["src/**/*.test.tsx", "src/**/*.test.ts"],
  },
  resolve: {
    alias: {
      "@": path.resolve(__dirname, "./src"),
      "@librepod/shared": path.resolve(__dirname, "../shared/src/types.ts"),
    },
  },
})
```
Note: Vitest runs independently from the Vite dev server, so @librepod/shared MUST be aliased here as a file path.

**ui/packages/client/index.html**:
```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>LibrePod — App Catalog</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```
  </action>
  <verify>
    <automated>cd /home/alex/code/librepod/marketplace/ui/packages/client && node -e "const v = require('./vite.config.ts'); console.log('ok')" 2>&1 || grep -l "tailwindcss" vite.config.ts && grep -l "jsdom" vitest.config.ts && grep -l "root" index.html && echo "files present"</automated>
  </verify>
  <acceptance_criteria>
    - ui/packages/client/vite.config.ts contains `tailwindcss from "@tailwindcss/vite"`
    - ui/packages/client/vite.config.ts contains `proxy: { "/api": "http://localhost:3000" }`
    - ui/packages/client/vite.config.ts contains `alias: { "@": path.resolve(__dirname, "./src") }`
    - ui/packages/client/vite.config.ts does NOT contain `@librepod/shared` (workspace symlink handles it)
    - ui/packages/client/vitest.config.ts contains `environment: "jsdom"`
    - ui/packages/client/vitest.config.ts contains `setupFiles: ["./src/test/setup.ts"]`
    - ui/packages/client/vitest.config.ts contains `"@librepod/shared": path.resolve(__dirname, "../shared/src/types.ts")`
    - ui/packages/client/vitest.config.ts contains `globals: true`
    - ui/packages/client/index.html contains `<div id="root"></div>`
    - ui/packages/client/index.html contains `src="/src/main.tsx"`
  </acceptance_criteria>
  <done>vite.config.ts, vitest.config.ts, and index.html written with correct content; all three files present and contain required strings.</done>
</task>

<task type="auto">
  <name>Task 3: Initialize shadcn and install components</name>
  <files>
    ui/packages/client/components.json
    ui/packages/client/src/index.css
    ui/packages/client/src/lib/utils.ts
    ui/packages/client/src/components/ui/badge.tsx
    ui/packages/client/src/components/ui/button.tsx
    ui/packages/client/src/components/ui/card.tsx
    ui/packages/client/src/components/ui/skeleton.tsx
    ui/packages/client/src/components/ui/separator.tsx
  </files>
  <read_first>
    - .planning/phases/02-catalog-ui/02-UI-SPEC.md (Design System section — init prompts: Style: Default, Base color: Slate, CSS variables: Yes)
    - .planning/phases/02-catalog-ui/02-RESEARCH.md (Pattern 1 — shadcn init in Vite, Anti-Patterns section for "use client" directive)
    - .planning/phases/02-catalog-ui/02-PATTERNS.md (No Analog Found section for these files)
  </read_first>
  <action>
Run shadcn init inside ui/packages/client/, then add the five required components.

Step 1 — Create a minimal src/main.tsx stub so shadcn init doesn't fail on missing entry point:
```typescript
// src/main.tsx (temporary stub)
export {}
```

Step 2 — Run shadcn init:
```bash
cd /home/alex/code/librepod/marketplace/ui/packages/client
npx shadcn@latest init --defaults
```
If `--defaults` is not available, run interactively and select:
- Style: Default (or New York — either works, pick Default)
- Base color: Slate
- CSS variables: Yes

Step 3 — Install the five required components:
```bash
cd /home/alex/code/librepod/marketplace/ui/packages/client
npx shadcn@latest add badge button card skeleton separator
```

Step 4 — Remove "use client" directives if present. After adding components, check each file in src/components/ui/ and strip the `"use client"` string if found (it is a Next.js artifact, harmless but messy in Vite):
```bash
for f in src/components/ui/*.tsx; do sed -i '/"use client"/d' "$f"; done
```

Step 5 — Verify src/index.css contains `@import "tailwindcss"` (Tailwind v4 syntax). If it contains `@tailwind base;` instead, replace the file with:
```css
@import "tailwindcss";

/* shadcn CSS variable tokens are written below by shadcn init */
```
(The rest of index.css — @layer base with :root and .dark CSS variables — must be preserved from shadcn init output.)

Step 6 — Verify src/lib/utils.ts exists and contains the cn() helper:
```typescript
import { clsx, type ClassValue } from "clsx"
import { twMerge } from "tailwind-merge"

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
```
If shadcn init wrote a different but equivalent implementation, leave it as-is.
  </action>
  <verify>
    <automated>cd /home/alex/code/librepod/marketplace/ui/packages/client && ls src/components/ui/ && grep -l "skeleton" src/components/ui/skeleton.tsx && grep "@import" src/index.css</automated>
  </verify>
  <acceptance_criteria>
    - ui/packages/client/components.json exists (shadcn config file created by init)
    - ui/packages/client/src/index.css exists and contains `@import "tailwindcss"` (Tailwind v4 syntax — NOT `@tailwind base`)
    - ui/packages/client/src/index.css contains `:root {` block with `--background:` and `--card:` CSS variables
    - ui/packages/client/src/index.css contains `.dark {` block with dark mode CSS variable overrides
    - ui/packages/client/src/lib/utils.ts exists and contains `clsx` import
    - ui/packages/client/src/lib/utils.ts contains `function cn(`
    - ui/packages/client/src/components/ui/badge.tsx exists
    - ui/packages/client/src/components/ui/button.tsx exists
    - ui/packages/client/src/components/ui/card.tsx exists (must export Card, CardContent, CardHeader)
    - ui/packages/client/src/components/ui/skeleton.tsx exists
    - ui/packages/client/src/components/ui/separator.tsx exists
    - None of the ui/*.tsx files contain `"use client"` string at top
  </acceptance_criteria>
  <done>shadcn initialized with Slate base color + CSS variables; five shadcn components (badge, button, card, skeleton, separator) present in src/components/ui/; src/index.css uses Tailwind v4 @import syntax with :root and .dark CSS variable blocks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Vite dev server | Local development only; dev server is not exposed beyond localhost |
| Vite proxy → NestJS :3000 | Internal proxy rewrite; /api requests forwarded to backend in dev mode |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-S01 | Spoofing | vite.config.ts proxy target | accept | Dev proxy only; not present in production build |
| T-02-T01 | Tampering | shadcn component copy-in | accept | Components are copied from official registry into repo; no runtime dependency on external registry |
</threat_model>

<verification>
After all three tasks complete:

1. `npm install` from ui/ completes with no peer dependency errors
2. `ls ui/packages/client/src/components/ui/` shows: badge.tsx, button.tsx, card.tsx, separator.tsx, skeleton.tsx
3. `grep "@import" ui/packages/client/src/index.css` outputs `@import "tailwindcss"` (not `@tailwind base`)
4. `grep "@librepod/shared" ui/packages/client/tsconfig.app.json` outputs the path alias
5. `grep "jsdom" ui/packages/client/vitest.config.ts` outputs the environment setting
6. `grep "proxy" ui/packages/client/vite.config.ts` outputs the /api proxy config
</verification>

<success_criteria>
- All npm dependencies installed (node_modules populated for @librepod/client)
- TypeScript path aliases `@/*` and `@librepod/shared` configured in BOTH tsconfig.json and tsconfig.app.json
- Vite config: @tailwindcss/vite plugin registered, /api proxy to localhost:3000, @/ alias to ./src
- Vitest config: jsdom environment, setupFiles pointing to src/test/setup.ts, @librepod/shared alias for Vitest resolver
- shadcn initialized: components.json present, src/index.css with Tailwind v4 @import + CSS variable tokens
- All five shadcn components (badge, button, card, skeleton, separator) in src/components/ui/
- No "use client" directives in component files
</success_criteria>

<output>
After completion, create `.planning/phases/02-catalog-ui/02-01-SUMMARY.md`
</output>
